# Модуль 33 Аксенов К.В.
#
# PageObject подход в реализации UI тестов с использованием Selenium + Pytest.
#
# Для запуска тестов нужно:
# 1. Установить pip3 install -r requirements
# 2. Загрузить актуальный драйвер Chrom https://chromedriver.chromium.org/downloads
# 3. Положить загруженный драйвер Chrom в корень проекта (с заменой, вместо имеющегося)
# 3. Использовать команду python -m pytest -v tests/tests_rostelekom.py
#
#    ATTENTION (ВНИМАНИЕ)!!!
# При запуске всех тестов может сработать защита Ростелеком, что приведет к блокировке IP
# Команда для запуска тестов
# Все тесты рабочие, падать не должны
# Если вдруг падает, значит скорее всего срабатывает защита Ростелеком
# Тесты можно запускать каждый индивидуально, напрямую из файла, для удобства проверки.

